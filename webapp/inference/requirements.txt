# --- API ---
# fastapi + uvicorn + watchfiles + httptools (via [standard] extras)
fastapi[standard]
 
# --- Settings ---
pydantic>=2
pydantic-settings
 
# --- Data Processing ---
numpy
Pillow
opencv-python-headless    # Postprocessing for masks and bounding boxes without GUI dependencies
pycocotools               # RLE encoding/decoding for masks
scipy                     # 3D bounding box fitting (PCA, convex hull, etc.)
 
# --- Progress & Logging ---
tqdm
